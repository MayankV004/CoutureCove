/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './views/**/*.ejs', // Adjust the path to your EJS templates
    './public/**/*.html', // Include static HTML if needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};